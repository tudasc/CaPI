if (ENABLE_INST_PASSES)
    add_subdirectory(passes)
endif()
add_subdirectory(selection)
add_subdirectory(driver)
add_subdirectory(calltree_logger)
add_subdirectory(symbol_retriever)

if (ENABLE_XRAY)
    add_subdirectory(xray)
endif()
