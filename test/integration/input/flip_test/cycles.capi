# TEST flip_cycles_per_invoc: 

flip_fns = has_flip_metrics(%%)
high_cycles = flip_cycles(">", 1000, %flip_fns)

!instrument(%high_cycles)

# capi -f cycles.capi --output-format json -o /dev/stdout flip_test.mcg