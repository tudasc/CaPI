#!/bin/bash
#

linking=yes
for arg in "$@" ; do
    case "$arg" in
    	-c|-S|-E|-M|-MM|-MMD)
            linking=no
    	    ;;
    esac
done

@LLVM_TOOLS_BINARY_DIR@/clang++ \
    -Xclang -load -Xclang @CAPI_LIB_DIR@/instrumentation_filter/instfilter.so \
	$LDFLAGS \
	$@
